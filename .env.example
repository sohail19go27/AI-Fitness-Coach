# Copy this file to `.env.local` and fill in your API keys and model IDs.
# Do NOT commit `.env.local` to source control. These are examples only.

# --- Gemini (Google) ---
# If you want to use Google Gemini for text generation, set these.
# You can omit GEMINI_* if you prefer OpenAI.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
GEMINI_API_URL=

# --- Replicate (Images) ---
# You must provide a Replicate API token and the model version id (not the model name).
REPLICATE_API_TOKEN=
REPLICATE_MODEL_VERSION=

# --- ElevenLabs (Text-to-Speech) ---
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# --- OpenAI (fallback) ---
# Optional: used when Gemini/Replicate aren't configured
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_URL=

# --- Other ---
# NODE_ENV=development
